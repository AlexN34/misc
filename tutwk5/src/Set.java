
public interface Set<E> {
	// ODO: add, remove, isAMember, union, intersection, subset
	// ODO: equals
	// ODO: iterator() returns iterator over sets using Iterable<E>
	// ODO: write test class using Scanner for file input
}

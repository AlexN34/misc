import java.util.Map;

public class Search {
	private Vertex<E> s;
	private Map<Vertex<E>, Integer> pathTable;
	public Search(Graph G, Vertex<E> s)  { // Search for vertices connected to s
		
	}
	public boolean marked(int v) { //is v connected to s?
		
	}
	int count()
	is v connected to s ?
	how many vertices are connected to s ?
}
